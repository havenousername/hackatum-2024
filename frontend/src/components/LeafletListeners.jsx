import { useMap } from 'react-leaflet'
import { useEffect } from 'react';

const LeafletListeners = () => {
    const map = useMap();

    useEffect(() => {
    }, []);
    return null;
};

export default LeafletListeners;