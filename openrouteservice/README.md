# OpenRouteService

To set up OpenRouteService (ORS), you need to run the [`setup.sh`](./setup.sh) script first.
Then, run `docker-compose up` and wait for a few min until ORS has compiled the graph.
